@using Syncfusion.Blazor.MarkdownConverter
@using Syncfusion.Blazor.RichTextEditor

<div class="container">
    <div class="row mb-3">
        <div class="col-md-12">
            <h3>Markdown Converter Options</h3>
            <div class="options-panel border p-3">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" @bind="enableGfm" @onchange="ConvertMarkdown" id="gfmOption" />
                    <label class="form-check-label" for="gfmOption">
                        Enable GitHub Flavored Markdown (GFM)
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" @bind="enableLineBreak" @onchange="ConvertMarkdown" id="lineBreakOption" />
                    <label class="form-check-label" for="lineBreakOption">
                        Convert single line breaks to &lt;br /&gt;
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" @bind="enableSilent" @onchange="ConvertMarkdown" id="silentOption" />
                    <label class="form-check-label" for="silentOption">
                        Silent Mode (suppress errors)
                    </label>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <h3>Markdown Input</h3>
            <SfRichTextEditor @bind-Value="markdownText" Height="300px" EditorMode="EditorMode.Markdown">
                 <RichTextEditorEvents ValueChange="@ValueChangeHandler"></RichTextEditorEvents>
            </SfRichTextEditor>
        </div>
        <div class="col-md-6">
            <h3>HTML Output</h3>
            <div class="preview-panel border p-3">
                @if (!string.IsNullOrEmpty(htmlOutput))
                {
                    <div>@((MarkupString)htmlOutput)</div>
                }
                else
                {
                    <p class="text-muted">HTML preview will appear here...</p>
                }
            </div>
        </div>
    </div>
</div>

@code {
    private string markdownText = @"# Heading 1

## Heading 2

This is a paragraph with **bold** and *italic* text.

- List item 1
- List item 2

| Column 1 | Column 2 |
|----------|----------|
| Cell 1   | Cell 2   |";
    
    private string htmlOutput = "";
    private bool enableGfm = true;
    private bool enableLineBreak = false;
    private bool enableSilent = false;

    protected override void OnInitialized()
    {
        ConvertMarkdown();
    }

    private void ConvertMarkdown()
    {
        var options = new MarkdownConverterOptions
        {
            Gfm = enableGfm,
            LineBreak = enableLineBreak,
            Silent = enableSilent
        };
        
        htmlOutput = SfMarkdownConverter.ToHtml(markdownContent, options);
    }
}